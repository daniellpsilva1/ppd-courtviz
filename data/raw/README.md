# Raw match fixtures

Source JSON for regenerating typed fixtures in `packages/data/src/fixtures/`.

```bash
node scripts/generate-fixtures.cjs
```

Do not import these files directly in app or package code — use `@courtviz/data/fixtures` instead.
